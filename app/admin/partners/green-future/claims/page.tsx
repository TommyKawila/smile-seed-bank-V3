import { GfClaimInboxClient } from "@/components/admin/partners/GfClaimInboxClient";

export const metadata = {
  title: "Seed claims | Green Future",
};

export default function GfClaimsInboxPage() {
  return <GfClaimInboxClient />;
}
